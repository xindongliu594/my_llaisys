# LLAISYS 双平台实现差异与性能对比

本文比较本项目在以下两个 CUDA/类 CUDA 平台上的实现方法和实测性能：

1. NVIDIA GeForce RTX 5090：原生 CUDA 12.8，完整 32 GiB GPU；
2. MetaX C500：MACA 3.3.0.15，容器分配 50% 计算资源和 32,000 MiB 显存配额。

两个平台都通过了 Runtime、Tensor、8 个算子以及
`DeepSeek-R1-Distill-Qwen-1.5B` 端到端推理测试。模型测试中，LLAISYS
生成的 token ID 与各平台 PyTorch 参考结果完全一致，直到 EOS。

> 注意：RTX 5090 使用完整 GPU，而 C500 是 50% 算力切片；PyTorch、驱动和
> 编译器版本也不同。因此跨平台延迟可以反映本次实际资源上的运行结果，但不能
> 当作两款完整 GPU 理论性能的严格对比。更公平的指标是同一平台内
> `PyTorch / LLAISYS` 的相对加速比。

## 一、总体结论

- 上层 LLAISYS API、Tensor、模型结构、权重加载流程和算子调度逻辑保持不变。
- RTX 5090 后端直接使用 CUDA Runtime、CUDA 数据类型和 cuBLAS，由 NVCC
  编译 `.cu` 文件。
- MetaX 后端使用 mxcc 编译 CUDA 风格 kernel，通过一个较薄的兼容层把
  CUDA Runtime 映射到 mcRuntime、cuBLAS 映射到 mcBLAS，并把 FP16/BF16
  类型映射到 MACA 类型。
- 8 个算子的核心 kernel 算法没有为 MetaX 重新复制一遍。MetaX 的 `.cpp`
  编译入口直接包含已经验证过的 NVIDIA `.cu` 实现，由 mxcc 重新编译成
  XCORE1000 目标代码。
- 在本次实测资源上，RTX 5090 的 LLAISYS 端到端推理耗时 0.44 秒；50% C500
  切片耗时约 1.00 秒。前者约快 2.27 倍，但这不是等资源比较。
- 相对于各自平台的 PyTorch，LLAISYS 在 RTX 5090 上加速 3.91 倍，在 C500
  切片上加速约 2.75 倍。

## 二、哪些部分完全相同

以下代码不需要按 GPU 厂商维护两份：

- Python API 和测试入口；
- `Tensor` 的 shape、stride、view、permute、slice、load 等逻辑；
- CPU Runtime 和 CPU 算子；
- Qwen2/DeepSeek 模型执行流程；
- safetensors 权重读取和装载流程；
- LLAISYS 对外设备枚举和算子调用接口；
- Add、Argmax、Embedding、Linear、RMSNorm、RoPE、Self-Attention、SwiGLU
  的 kernel 算法。

课程给出的公开 ABI 只有 CPU 和 NVIDIA 类型。为了不修改 Python API 和已有
测试，MetaX 这种 CUDA 类设备在上层仍使用：

```bash
--device nvidia
```

这只是 LLAISYS 的公开设备类型名称，并不表示底层实际调用了 NVIDIA 驱动。

## 三、实现差异

| 对比项 | RTX 5090 | MetaX C500 |
| --- | --- | --- |
| 编程/软件栈 | NVIDIA CUDA 12.8 | MetaX MACA 3.3.0.15 |
| 编译器 | NVCC，由 Xmake CUDA rule 调用 | mxcc 1.0.0，自定义 Xmake toolchain |
| 目标架构 | `sm_120` | `XCORE1000`，使用 `-offload-arch native` |
| Runtime | CUDA Runtime | mcRuntime |
| BLAS | cuBLAS | mcBLAS |
| FP16 类型 | CUDA `__half` | MACA FP16，通过兼容头映射 |
| BF16 类型 | CUDA `__nv_bfloat16` | MACA `__maca_bfloat16`，通过别名映射 |
| Kernel 源文件 | 直接编译 `src/ops/*/nvidia/*.cu` | 编译 `src/ops/*/metax/*.cpp`，入口包含对应 `.cu` |
| 构建开关 | `--nv-gpu=y` | `--nv-gpu=n --metax-gpu=y` |
| 链接库 | `cublas` 和 CUDA Runtime | `mcblas`、`mcruntime` |
| 运行库查找 | CUDA 默认路径 | 为 `/opt/maca/lib` 添加 link path 和 rpath |

### 3.1 NVIDIA 原生实现

NVIDIA 后端的 Runtime 直接调用：

- `cudaGetDeviceCount`、`cudaSetDevice`；
- `cudaMalloc`、`cudaFree`、`cudaMallocHost`；
- `cudaMemcpy`、`cudaMemcpyAsync`；
- `cudaStreamCreate`、`cudaStreamSynchronize`；
- `cublasGemmEx`。

Xmake 使用 CUDA rule 编译 `.cu` 文件，并启用 device link。主要文件是：

```text
xmake/nvidia.lua
src/device/nvidia/nvidia_runtime_api.cu
src/device/nvidia/nvidia_resource.cu
src/ops/*/nvidia/*.cu
```

### 3.2 MetaX 兼容实现

MetaX 后端没有修改上层 Runtime 接口，而是在编译期把 CUDA 名称映射到 MACA：

```text
cudaGetDeviceCount  -> mcGetDeviceCount
cudaSetDevice       -> mcSetDevice
cudaMalloc          -> mcMalloc
cudaMemcpy          -> mcMemcpy
cudaStreamCreate    -> mcStreamCreate
cublasGemmEx        -> mcblasGemmEx
```

兼容层位于：

```text
src/device/metax/compat/cuda_runtime.h
src/device/metax/compat/cuda_fp16.h
src/device/metax/compat/cuda_bf16.h
src/device/metax/compat/cublas_v2.h
```

每个 MetaX 算子入口只负责让 mxcc 使用兼容头重新编译现有 kernel。例如：

```cpp
// src/ops/linear/metax/linear_metax.cpp
#include "../nvidia/linear_nvidia.cu"
```

这种设计的优点是两平台共享算法和修复，避免两份 kernel 后续逐渐不一致；平台
相关差异集中在兼容头、编译工具链和链接设置中。

### 3.3 Linear 的 MetaX 特殊处理

最初把 CUDA 的 `CUBLAS_COMPUTE_32F_PEDANTIC` 原样映射到 mcBLAS 的 pedantic
模式时，C500 的大矩阵 FP32 Linear 耗时为 16.31 ms。分析表明该模式没有进入
C500 的优化 GEMM 路径。

因此 MetaX 兼容层把它映射为标准 `MCBLAS_COMPUTE_32F`。修改后：

- FP32 Linear 从 16.31 ms 降到 0.643 ms，约提升 25.4 倍；
- Float32、Float16、BFloat16 的正确性测试仍全部通过；
- 端到端模型 token 仍与 PyTorch 完全一致。

该修改只影响 MetaX 的 mcBLAS 兼容层，不改变 NVIDIA cuBLAS 的行为。

## 四、算子性能对比

测试方法：预热 10 次，再同步执行 100 次并取平均值。表中的“平台内加速比”为
`PyTorch 耗时 / LLAISYS 耗时`，大于 1 表示 LLAISYS 更快。“C500/RTX 延迟比”
为两个平台的 LLAISYS 耗时之比。

| 算子/规模 | RTX PyTorch (ms) | RTX LLAISYS (ms) | RTX 平台内加速比 | C500 PyTorch (ms) | C500 LLAISYS (ms) | C500 平台内加速比 | C500/RTX LLAISYS 延迟比 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Add，`512×4096`，FP32 | 0.00476 | 0.00467 | 1.02× | 0.02288 | 0.03455 | 0.66× | 7.40× |
| Argmax，`4096`，FP32 | 0.00483 | 0.00374 | 1.29× | 0.01301 | 0.01456 | 0.89× | 3.89× |
| Embedding，`50×4096`，FP32 | 0.01156 | 0.00277 | 4.17× | 0.05417 | 0.01907 | 2.84× | 6.88× |
| RMSNorm，`512×4096`，FP32 | 0.02436 | 0.00732 | 3.33× | 0.08539 | 0.03575 | 2.39× | 4.88× |
| RoPE，`512×4×4096`，FP32 | 0.11198 | 0.02121 | 5.28× | 0.35895 | 0.17130 | 2.10× | 8.08× |
| SwiGLU，`512×4096`，FP32 | 0.02193 | 0.00472 | 4.65× | 0.09300 | 0.03453 | 2.69× | 7.32× |
| Self-Attention，`q=5,kv=11,h=4`，FP32 | 0.11240 | 0.00392 | 28.67× | 0.14996 | 0.01465 | 10.24× | 3.74× |
| Linear，`512×4096×4096`，FP32 | 0.30145 | 0.30945 | 0.97× | 0.60225 | 0.64319 | 0.94× | 2.08× |
| Linear，`512×4096×4096`，FP16 | 0.09454 | 0.09875 | 0.96× | 0.14869 | 0.17653 | 0.84× | 1.79× |
| Linear，`512×4096×4096`，BF16 | 0.09349 | 0.09771 | 0.96× | 0.14974 | 0.17651 | 0.85× | 1.81× |

### 4.1 如何理解算子数据

1. **RTX 5090 的绝对延迟整体更低。** 在当前实际资源下，RTX 版本的
   LLAISYS 算子比 50% C500 切片快约 1.79～8.08 倍，具体取决于算子的计算量、
   访存模式和 kernel 启动开销。
2. **自定义融合/专用 kernel 在两边都有效。** Embedding、RMSNorm、RoPE、
   SwiGLU 和小规模 Self-Attention 相对于各平台 PyTorch 都有明显优势。
3. **Add 和 Argmax 在 C500 上没有超过 PyTorch。** 这类 kernel 很短，固定的
   启动和同步开销占比很高；目前实现也没有针对 MACA 单独调整 block 大小或做
   多算子融合。
4. **Linear 已接近厂商 PyTorch。** RTX 上 LLAISYS 比 PyTorch 慢约 2.7%～4.5%；
   C500 上慢约 6.8%～18.7%。LLAISYS 和 PyTorch 最终都调用厂商 BLAS，因此差距
   主要来自参数选择、框架封装、算法选择和同步开销，而不是手写矩阵乘 kernel。
5. **Self-Attention 的倍率不能直接推广到长序列。** 当前性能用例只有
   `q=5, kv=11`，PyTorch 通用算子的调度开销较大，而 LLAISYS kernel 针对作业
   形状更加直接。序列变长后应重新测试吞吐量和显存使用。

## 五、端到端模型性能

模型：`DeepSeek-R1-Distill-Qwen-1.5B`，BF16，greedy decoding，最多 128 步，
两套实现生成相同 token 并在相同位置遇到 EOS。

| 平台 | 资源条件 | PyTorch (s) | LLAISYS (s) | 平台内加速比 |
| --- | --- | ---: | ---: | ---: |
| RTX 5090 | 完整 GPU，32 GiB | 1.72 | 0.44 | 3.91× |
| MetaX C500 | 50% 计算切片，32,000 MiB 配额 | 2.75～2.76 | 0.99～1.00 | 约 2.75× |

从绝对时间看，RTX 5090 上的 LLAISYS 比当前 C500 切片约快 2.27 倍。从平台内
对照看，两个后端均明显快于对应的 PyTorch 参考执行，说明 Runtime、Tensor、
算子和模型执行层的组合优化在两种软件栈上都能工作。

端到端加速并不等于每个算子都同比加速。模型逐 token 解码包含大量小 kernel、
Python/框架调度和通用 PyTorch 模型开销；LLAISYS 的执行路径更短，因此即使
Linear 单项略慢于厂商 PyTorch，完整推理仍能获得明显收益。

## 六、构建与复现命令

### 6.1 RTX 5090

```bash
xmake f -c -m release --nv-gpu=y --cuda=/usr/local/cuda
xmake build -j 16
xmake install
export PYTHONPATH="$PWD/python:$PWD"
python test/test_runtime.py --device nvidia
python test/ops/linear.py --device nvidia --profile
python test/test_infer.py \
    --model /path/to/DeepSeek-R1-Distill-Qwen-1.5B \
    --device nvidia --test --max_steps 128
```

### 6.2 MetaX C500

```bash
export XMAKE_ROOT=y
export MACA_PATH=/opt/maca
export LD_LIBRARY_PATH="/opt/maca/lib:/opt/mxdriver/lib:$LD_LIBRARY_PATH"
xmake f -c -m release --nv-gpu=n --metax-gpu=y
xmake build -j 16
xmake install
export PYTHONPATH="$PWD/python:$PWD"
python test/test_runtime.py --device nvidia
python test/ops/linear.py --device nvidia --profile
python test/test_infer.py \
    --model /root/models/DeepSeek-R1-Distill-Qwen-1.5B \
    --device nvidia --test --max_steps 128
```

## 七、最终判断

本项目采用的是“统一上层接口和统一 kernel 算法，平台相关工具链与运行库适配”
的实现方式。NVIDIA 是原生 CUDA 基准后端；MetaX 通过较薄的 MACA 兼容层复用
同一套 CUDA 风格实现。这样既完成了两平台适配，也避免为每个平台维护独立的
模型和算子逻辑。

当前 RTX 5090 的绝对性能更高；在 50% C500 切片上，MetaX 后端仍实现了完整
正确性并在端到端推理中比其 PyTorch 参考快约 2.75 倍。下一步如果继续优化
MetaX，应优先处理短 kernel 的启动开销、block 参数自动调优、算子融合，以及
mcBLAS 的算法/工作区选择。
