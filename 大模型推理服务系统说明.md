# LLAISYS 大模型推理服务系统说明

## 1. 系统定位

本项目不仅实现 Qwen2/DeepSeek-R1-Distill-Qwen 模型推理，也提供一个单机、无数据库、
OpenAI 风格的大模型推理服务。系统重点包括：C/C++/CUDA 后端、多会话、KV Cache、
Orca Continuous Batching、过载保护、可观测性和稳定性验证。

按照项目范围，系统不实现网页、用户登录、数据库、多机分布式、Tensor Parallel、INT4、
Speculative Decoding。

## 2. 请求执行路径

```text
HTTP 请求
   ↓ 参数与上下文校验
RequestPool（优先级、队列容量、排队超时）
   ↓
OrcaScheduler（接纳、Selective Prefill、动态 Decode Batch）
   ↓
Qwen2 C++/CUDA（每个 Sequence 独立 KV Cache）
   ↓
TokenEvent → SSE / 非流式响应 / 请求结果 / Metrics
```

请求阶段为：

```text
WAITING → PREFILL → DECODE → FINISHED
                     ├─ CANCELLED
                     ├─ TIMEOUT
                     └─ FAILED
```

## 3. 已实现功能

### 推理和调度

- OpenAI 风格 Chat Completion 和 Text Completion；
- 非流式和 SSE 流式输出；
- Argmax、Top-K、Top-P、Temperature、Repetition Penalty 和 Seed；
- `min_tokens`、`ignore_eos`、Stop Token ID 和跨 Token Stop 字符串；
- 每个请求独立 KV Cache；
- Orca 迭代级调度、Selective Batching 和动态 Continuous Batching；
- 请求结束、取消、超时或失败后立即释放 Sequence 和 KV Cache；
- C++ 后端按需返回选中 Token Logprob 和 Top-N Logprobs。

### 请求和会话

- 内存会话创建、查询、列表、删除、导入和导出；
- 同一会话仅允许一个在途请求；
- 请求查询、列表、状态筛选和主动取消；
- 请求优先级、最大等待队列和最大活跃请求数；
- 服务 Drain 和安全关闭。

### 可靠性保护

- 模型最大上下文检查和可选左截断；
- 总超时、排队超时、Prefill 超时和 Decode 超时；
- SSE 客户端断开后自动取消请求；
- KV Cache 精确容量估算、预算预留和高水位拒绝；
- 单请求后端异常/OOM 隔离，调度线程继续运行；
- 429 过载和 503 服务不可用返回。

Prefill/Decode 超时无法强制中断正在执行的同步 CUDA Kernel，而是在 Kernel 返回后立即
终止请求并释放资源。真正的 Kernel 抢占需要异步执行和更底层的取消机制。

## 4. HTTP 接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/health` | GET | 进程存活状态 |
| `/ready` | GET | 是否仍接收新请求 |
| `/metrics` | GET | Prometheus 文本指标 |
| `/v1/models` | GET | 模型列表 |
| `/v1/chat/completions` | POST | Chat 推理 |
| `/v1/completions` | POST | Text Completion |
| `/v1/tokenize` | POST | 文本或 Chat Template Token 计数 |
| `/v1/detokenize` | POST | Token ID 解码 |
| `/sessions` | GET/POST | 会话列表和创建 |
| `/sessions/import` | POST | 导入会话 |
| `/sessions/{id}` | GET/DELETE | 查询或删除会话 |
| `/sessions/{id}/messages` | POST | 向持久会话提交消息 |
| `/requests` | GET | 请求列表与筛选 |
| `/requests/{id}` | GET | 请求结果、阶段和耗时 |
| `/requests/{id}/cancel` | POST | 主动取消请求 |

## 5. 启动命令

```bash
python -m llaisys.serve \
  --model /path/to/DeepSeek-R1-Distill-Qwen-1.5B \
  --device nvidia \
  --scheduler orca \
  --host 127.0.0.1 \
  --port 8000 \
  --max-queue-size 256 \
  --max-active-requests 32 \
  --max-prefill-per-iteration 1 \
  --max-kv-cache-mib 8192 \
  --kv-cache-high-watermark 0.9 \
  --log-level INFO
```

`--max-kv-cache-mib 0` 表示不设置软件预算。生产测试建议显式设置，并为模型临时张量、
CUDA 库工作区和驱动保留显存余量。

## 6. 请求示例

```bash
curl http://127.0.0.1:8000/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{
    "model": "DeepSeek-R1-Distill-Qwen-1.5B",
    "messages": [{"role": "user", "content": "解释 KV Cache"}],
    "stream": true,
    "max_tokens": 64,
    "min_tokens": 4,
    "temperature": 0.8,
    "top_p": 0.9,
    "logprobs": true,
    "top_logprobs": 5,
    "queue_timeout_seconds": 5,
    "prefill_timeout_seconds": 30,
    "decode_timeout_seconds": 60,
    "timeout_seconds": 90
  }'
```

Token 计数：

```bash
curl http://127.0.0.1:8000/v1/tokenize \
  -H 'Content-Type: application/json' \
  -d '{
    "model": "DeepSeek-R1-Distill-Qwen-1.5B",
    "messages": [{"role": "user", "content": "你好"}]
  }'
```

## 7. 指标和日志

`/metrics` 包含：

- 请求总数、成功、失败、取消、活跃数量；
- 输入/输出 Token 和吞吐量；
- TTFT、总耗时、单请求 Token Rate 的 P50/P95/P99；
- Queue、Prefill、Decode 的 P50/P95/P99；
- 当前等待请求和活跃 Sequence；
- KV Cache 容量、预算、已预留、已分配、可用量和拒绝次数；
- 后端错误和 OOM 次数；
- Orca 迭代次数、Prefill/Decode Batch 当前值、最大值和平均值。

每个终止请求输出一条 JSON 日志，包括 Request ID、Session ID、状态、结束原因、Token 数、
Queue/Prefill/Decode/TTFT/总耗时和错误信息。

## 8. Benchmark

普通并发测试：

```bash
python -m llaisys.benchmark \
  --endpoint http://127.0.0.1:8000 \
  --model DeepSeek-R1-Distill-Qwen-1.5B \
  --requests 100 --concurrency 8 --max-tokens 64
```

混合负载和取消测试：

```bash
python -m llaisys.stress_benchmark \
  --endpoint http://127.0.0.1:8000 \
  --model DeepSeek-R1-Distill-Qwen-1.5B \
  --requests 1000 \
  --concurrency 16 \
  --prompt-words 16,128,512 \
  --output-lengths 8,32,128 \
  --cancellation-ratio 0.1 \
  --output stress_result.json
```

持续 30 分钟：

```bash
python -m llaisys.stress_benchmark \
  --endpoint http://127.0.0.1:8000 \
  --model DeepSeek-R1-Distill-Qwen-1.5B \
  --duration-seconds 1800 \
  --concurrency 16 \
  --cancellation-ratio 0.1 \
  --output soak_30min.json
```

工具会区分 Completed、Cancelled、Overloaded 和 Failed，并在结束后检查活跃 Sequence 与
KV Cache 预留是否归零。

## 9. 当前实现边界

- KV Cache 仍是每个 Sequence 的连续内存，不是 Paged KV Cache；
- 变长 Self-Attention 仍按 Sequence 调用，尚未融合为 Paged/Variable-Length Attention；
- 没有 Prefix Cache；
- 会话保存在内存，进程重启后需要重新导入；
- 当前单模型、单机、单设备推理，不包含多模型热加载和分布式能力。

下一步如果继续提升技术深度，优先顺序是 Paged KV Cache、Prefix Cache、融合变长
Attention，然后再做更长时间的真实 GPU 稳态测试。
